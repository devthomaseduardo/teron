# Variaveis opcionais para docker compose (raiz)
# Segredos reais: use apps/api/.env e o painel da Vercel — NUNCA commit.

RESEND_API_KEY=
RESEND_FROM=TERON <onboarding@resend.dev>
MERCADOPAGO_ACCESS_TOKEN=

# Mongo: so a connection string (mongodb+srv://...)
# MONGODB_URI=
